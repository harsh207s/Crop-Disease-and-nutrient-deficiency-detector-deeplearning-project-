import tensorflow as tfimport tensorflow as tf

model = tf.keras.models.load_model("model")   # folder containing saved_model.pb
model.save("model.keras")


model = tf.keras.models.load_model("model")   # folder containing saved_model.pb
model.save("model.keras")
